<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{ path('accueil.index') }}">Solarize</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarColor01">
			<ul class="navbar-nav me-auto">
				{% if app.user %}
					<li class="nav-item">
						<a href="{{ path('modification') }}" class="btn btn-primary me-2">Modifier</a>
					</li>
				</ul>
				<ul class="navbar-nav d-flex align-items-center">
					<span class="navbar-text me-3 user-name">
						Bienvenue,
						{{ app.user.username }}!
					</span>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
					</li>
				</ul>
			{% else %}
				<li class="nav-item">
					<a href="{{ path('app_login') }}" class="btn btn-primary me-2">Connexion</a>
				</li>
				<li class="nav-item">
					<a href="{{ path('app_register') }}" class="btn btn-primary me-2">Inscription</a>
				</li>
			</ul>
		{% endif %}
	</div>
</div></nav><style>
.user-name {
	font-size: 18px;
	font-weight: bold;
	color: #fff;
}</style>
